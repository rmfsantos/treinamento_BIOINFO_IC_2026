bash
cat << 'EOF' > ~/.condarc
channels:
  - defaults
show_channel_urls: true

proxy_servers:
  http:  http://proxy.uefs.br:3128
  https: http://proxy.uefs.br:3128

ssl_verify: true
EOF



bash
conda clean -i
conda create --name orchid_qc python=3.11



conda config --add channels defaults
conda config --add channels conda-forge
conda config --add channels bioconda
conda config --set channel_priority strict
